# Rebuilt inside the image; a stale local copy must never leak in.
node_modules
dist

# History, screenshots and docs are not part of the running game.
.git
.gitignore
docs
shots
*.log
.DS_Store

# The container recipe itself.
Dockerfile
.dockerignore
docker-compose.yml
