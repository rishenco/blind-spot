server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # The build is a single self-contained HTML file that changes every batch;
    # never let the browser serve a stale one.
    add_header Cache-Control "no-store";

    location / {
        try_files $uri /index.html;
    }
}
